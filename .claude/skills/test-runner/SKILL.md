---
name: test-runner
description: >-
  Run unit, integration, and end-to-end test suites with pytest, manage mock
  fixtures, and enforce strict per-file coverage >= 90%.
---

# test-runner

This skill lives in the workspace skill set shared with every agent, at:

- `.agents/skills/test-runner/SKILL.md`

Read that file and follow it. It is the single source of truth; this one only
makes it invocable as `/test-runner` in Claude Code.
