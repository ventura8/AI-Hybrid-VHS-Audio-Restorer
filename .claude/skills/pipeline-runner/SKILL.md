---
name: pipeline-runner
description: >-
  Execute and diagnose the canonical local pipeline runner
  (run_pipeline_locally.sh on Linux/macOS, run_pipeline_locally.ps1 on
  Windows), validate quality gates, and regenerate coverage badges.
---

# pipeline-runner

This skill lives in the workspace skill set shared with every agent, at:

- `.agents/skills/pipeline-runner/SKILL.md`

Read that file and follow it. It is the single source of truth; this one only
makes it invocable as `/pipeline-runner` in Claude Code.
