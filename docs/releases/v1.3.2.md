# v1.3.2

## Highlights

A patch release about temp and file hygiene: every stage now publishes its
file atomically, the final render is staged inside the work directory, a
resumed run sweeps the partials an interrupted run left, and the process's
temp locations are confined to the work directory. Restoration output is
unchanged: `cathar` is bit-identical to the v1.3.1 build on all five
reference clips, and a run cut off three times and resumed produces the same
decoded audio as an uninterrupted run.

- **Partials could be resumed as finished stages.** Stages streamed
  straight into their target file: the FFmpeg polish, de-esser and expander
  renders, the pop repair, the hum and tone cancellers, the plosive tamer,
  the native spectral suppressor, DeepFilterNet and the learned blend. A
  power cut mid-write leaves a WAV whose header still reads as valid audio
  (a test confirms a file truncated to a third passes `is_valid_audio`), so
  the next run would have taken the fragment for the whole and carried it
  through to the output. Every one of them now writes through
  `atomic_target()` and publishes with one rename.
- **The final render bled next to the source.** The mux wrote
  `<name>_Cleaned.tmp.<ext>` beside the video and renamed it on success; an
  interruption left that file outside the work directory. It is now staged
  inside `.temp_work_<video>/` and moved into place only once verified.
- **A skipped task never tidied up.** A run cut off between publishing the
  output and removing its work directory left that directory forever: the
  next run saw the output, skipped the task, and returned before cleanup.
  The skip path now removes a stale work directory too.
- **Partials are swept on resume.** Reopening a work directory removes every
  unpublished partial (`*.tmp.*` and the dot-prefixed same-directory temp
  `audio-separator` leaves), so no stage can glob one as its own output.
- **Temp is confined.** While a video is processed, Python's `tempfile` and
  the `TMP`/`TEMP`/`TMPDIR` variables every child process inherits point at
  `.temp_work_<video>/tmp`, so library scratch leaves with the work
  directory instead of accumulating in the system temp.
- **Windows cleanup retries.** Removing the work directory retries briefly
  for handles the OS releases late and warns when it cannot, instead of
  failing silently.
- **The session log is capped.** `session_log.txt` grew without bound (53 MB
  on the development machine); it now rotates at 10 MB, keeping one previous
  generation.
- The launcher no longer creates an unused `output/` folder in the launch
  directory; restored videos have always been written next to their source.
- **Three-hour captures restore.** The UVR denoiser keeps several full-length
  copies of the track in memory after inference, so a 3h00m capture failed
  outright on a 62 GB machine where 2h11m finished, at about 30 GB of host
  memory per hour of audio. A track longer than the chunk length, sized from
  the machine's memory at that rate (57 minutes on a 62 GB desktop, 28 on a
  30 GB laptop) unless `neural_chunk_seconds` fixes it, is now denoised in
  equal chunks and rejoined, and the chunking is built to reproduce the
  whole-file result
  rather than approximate it: chunks start on the models' patch stride, each
  is led in by the file's loudest block so the model normalises it as it
  would the whole file, each carries a stride of true context either side,
  and the seams are two-second linear crossfades. Measured on an 88-minute
  capture cut in three, the join matches the whole-file output on 99.97% of
  samples with the rest at float rounding; body -103 dB, seams -156 dB
  relative to the signal; a chunk the separator peak-limited on its own is
  denoised again at half scale and undone at the join, so no seam steps in
  level. Tracks up to the chunk length are processed whole and bit-identical
  to before. See `docs/configuration.md`, "Long Captures".

## Verification

- Default mode on `E:\Test` (two captures): only the two `*_Cleaned` files
  appeared next to the sources; no work directory, no file in the launch
  directory beyond `session_log.txt`, nothing new in the system temp.
- Simulated power loss on the default mode, killed during a partial write,
  two seconds into a later stage, and while the final render was staged: the
  partial left each time was removed on the next start with a `[Hygiene]`
  log line, nothing was ever left beside the source, and the resumed output
  decoded identically to an uninterrupted run.
- Every other mode was run on the same folder with a filesystem snapshot
  before and after; the findings are in the pull request.
- `E:\Test full`, 21 captures and 90 GB: 21 of 21 restored, each removing its
  work directory. The two three-hour captures, which had failed on every
  attempt, restored in 24.5 and 27.8 minutes: the 3h04m one in two 92-minute
  chunks under a fixed two-hour setting, the 3h00m one in four 57-minute
  chunks under the machine-sized default, two of which the separator
  peak-limited and the runner denoised again at half scale.
- Two clean-system installs of the Linux installer: a Docker image from
  `ubuntu:24.04` with apt's Python 3.12, and a WSL2 Ubuntu 26.04 machine
  with a uv-provided 3.12. Both provisioned FFmpeg, cathar, the ML stack with
  CUDA and Resemble-Enhance, and created `input/` but no `output/`.
- The 88-minute capture in memory-limited containers on the same GPU, with
  the chunk length sized from the container's limit: 16 GB restored it in six
  fifteen-minute chunks (25.7 min); 12 GB was killed inside a fifteen-minute
  chunk, and restored it in nine eleven-minute chunks once the floor was
  lowered to five minutes; 8 GB was killed after the spectral subtraction,
  before the chunked denoiser was reached. Every run left zero partials.
- A `kill -9` mid-run on the container's ext4 volume, then a resume: the
  extraction, pre-conditioning and cathar stages were reused and the
  restoration finished in 16.6 minutes with zero partials.
- A publish across filesystems, from the ext4 volume to a Windows bind mount
  (device 2080 to device 68): the rename could not cross, the copy-and-rename
  fallback ran, the 1.6 GB output arrived, nothing partial was left on the
  target, and the work directory was removed.
- `cathar` identity: 5 of 5 reference clips decode bit-identically to the
  v1.3.1 outputs.
- Quality gate: 1117 tests, 95.21% total coverage, every file at or above
  90%, Radon CC and MI grade A, ruff / black / isort / flake8 / pylint /
  bandit clean.

## Measured performance

Every mode was run end to end through the generated 45 s fixture on two
accelerators, since the staged final render and the atomic publish sit in
the path all ten modes share. All 40 executions (ten modes, two fixtures,
two machines) succeeded.

| Machine | Mode | Real-time factor | Peak VRAM |
| :--- | :--- | ---: | ---: |
| RTX 5090, 32 GB (batch 32) | `auto_pure_linear` | 5.60x | 18,723 MB |
| RTX 5090, 32 GB (batch 32) | `auto` | 2.16x | 19,255 MB |
| RTX 5090, 32 GB (batch 32) | `multipass_auto` | 2.17x | 19,255 MB |
| RTX 5090, 32 GB (batch 32) | `auto_pure` | 3.03x | 19,255 MB |
| RTX 5090, 32 GB (batch 32) | `cathar` | 1.19x | 1,903 MB |
| RTX 5090, 32 GB (batch 32) | `hybrid` | 1.89x | 19,255 MB |
| RTX 5090, 32 GB (batch 32) | `denoise_only` | 9.11x | 18,723 MB |
| RTX 5090, 32 GB (batch 32) | `auto_ffmpeg_native` | 27.95x | 1,903 MB |
| RTX 5090, 32 GB (batch 32) | `vhs_native` | 29.70x | 1,903 MB |
| RTX 5090, 32 GB (batch 32) | `arnndn_speech` | 20.28x | 1,903 MB |
| RTX 3080 Laptop, 8 GB (batch 1) | `auto_pure_linear` | 0.74x | 1,039 MB |
| RTX 3080 Laptop, 8 GB (batch 1) | `auto` | 0.63x | 7,710 MB |
| RTX 3080 Laptop, 8 GB (batch 1) | `multipass_auto` | 0.62x | 7,710 MB |
| RTX 3080 Laptop, 8 GB (batch 1) | `auto_pure` | 0.96x | 3,157 MB |
| RTX 3080 Laptop, 8 GB (batch 1) | `cathar` | 0.76x | 373 MB |
| RTX 3080 Laptop, 8 GB (batch 1) | `hybrid` | 0.62x | 7,710 MB |
| RTX 3080 Laptop, 8 GB (batch 1) | `denoise_only` | 4.87x | 1,039 MB |
| RTX 3080 Laptop, 8 GB (batch 1) | `auto_ffmpeg_native` | 8.83x | 373 MB |
| RTX 3080 Laptop, 8 GB (batch 1) | `vhs_native` | 13.30x | 373 MB |
| RTX 3080 Laptop, 8 GB (batch 1) | `arnndn_speech` | 6.76x | 373 MB |

On the 8 GB card the Resemble-Enhance modes peak within 500 MB of the
ceiling and finish; the batch size the hardware profile selects is what
keeps them there, where the 32 GB card runs the same modes at 19 GB. The
same 40 executions were repeated on the final build, after the chunked
denoiser landed, and passed 40 of 40; the 5090 figures were unchanged and the
laptop's were taken under a load average of 40 from other work, so the table
keeps the earlier, uncontended measurement.

## Upgrade Notes

No configuration change. A work directory left by an earlier build is
resumed and cleaned by this one. The launch directory may still contain an
empty `output/` folder from earlier builds; it is safe to delete.

**Full Changelog**:
<https://github.com/ventura8/AI-Hybrid-VHS-Audio-Restorer/compare/v1.3.1...v1.3.2>
