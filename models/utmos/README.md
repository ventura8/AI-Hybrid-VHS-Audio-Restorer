# utmos

Fetched by `scripts/download_quality_models.py` from https://github.com/tarepan/SpeechMOS
(revision `tarepan/SpeechMOS:v1.2.0`).

Licence: MIT (tarepan/SpeechMOS, sarulab-speech/UTMOS22).

`MANIFEST.json` holds the sha256 of every file as observed on first fetch; a later
fetch that hashes differently is refused.
